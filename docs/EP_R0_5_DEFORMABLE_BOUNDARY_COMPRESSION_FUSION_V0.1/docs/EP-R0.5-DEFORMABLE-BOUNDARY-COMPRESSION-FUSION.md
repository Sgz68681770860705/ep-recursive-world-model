# EP-R0.5: Deformable Phase Boundary, Compression Memory, and Fusion Diagnostics

Status:

`[DERIVED IN THE CANONICAL LOG-SHAPE MODEL] + [EXPLORATORY NUMERICAL — ALL FROZEN GATES PASSED] + [IDENTITY RECONSTRUCTION OPEN]`

Finite boundaries now carry canonical log-shape variables,

\[
a_i=a_{i0}e^{\beta_{ia}},
\qquad
b_i=b_{i0}e^{\beta_{ib}},
\]

which guarantee positive axes and allow shape, overlap, and rotational inertia to evolve in one Hamiltonian ledger.

The benchmark verifies reversible exchange among translational, overlap, shape, and spin energies. Nonzero shape oscillations persist after the overlap interval, providing reversible compression memory rather than irreversible relaxation.

In the frozen exploratory parameter window, a faster centered approach produced a larger peak repulsive force and substantially larger shape-energy storage than a slower approach.

An unlabeled geometric field is defined by

\[
\chi_U=1-(1-\chi_1)(1-\chi_2).
\]

At the frozen superlevel threshold \(0.10\), the tested collision followed a connected-component sequence

\[
2\rightarrow1\rightarrow2,
\]

which is classified as temporary geometric fusion followed by labeled scattering.

The dynamics still retain two canonical objects. The result does not establish identity destruction, permanent fusion, or reconstruction into genuinely new outgoing structures.

The topology and memory thresholds were calibrated during exploration and then frozen. The package is reproducible but is not an independent confirmatory preregistration.
